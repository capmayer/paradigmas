Então, não esta funcionando muito bem.

A parte de geração do HTML está ok.

O problema: salvar e dar load nos arquivos.
O problema2: usar o table do FLTK para inserir as diversas colunas que o template poderia suportar.

Titlo e subtitlo são alterar corretamente, dois campos ficticios são utilizados para testar a geração.
